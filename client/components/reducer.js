// const SWITCH_FLAG = 'SWITCH_FLAG'

// export default function (state, action) {
//   switch (action.type) {
//     case SWITCH_FLAG:
//       return { ...state, [action.checboxInfo]: !state.action[checboxInfo] }
//     default: {
//       return state
//     }
//   }
// }
